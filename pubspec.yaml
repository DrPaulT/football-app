name: football_app
description: A technical test example to display data from a football competition.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.4
  get_it: ^7.2.0
  http_mock_adapter: ^0.3.2
  intl: ^0.17.0
  json_annotation: ^4.4.0
  provider: ^6.0.2

dev_dependencies:
  build_runner: ^2.1.8
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  json_serializable: ^6.1.5

flutter:
  uses-material-design: true
